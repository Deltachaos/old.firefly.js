module.exports = function(App, Firefly) {
	
	var fs = App.load('Utility', 'Filesystem');
	
	return new Class({
		initialize: function() {
			/*
			var CaseFolder = new fs(App.path());
			CaseFolder.readdir(function(err, values) {
				console.log(values);
			});
			*/
		}
	});
	
};